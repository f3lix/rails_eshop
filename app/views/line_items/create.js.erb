$('#top-cart').html("<%=j link_to('Shopping Cart (' + number_to_currency(current_cart.total_price) + ')', { controller: 'carts', action: 'show', id: session[:cart_id] }) %>")

$('#top-cart').css({'font-size' : '20px'}).animate({'font-size' : '14px'}, 1000)